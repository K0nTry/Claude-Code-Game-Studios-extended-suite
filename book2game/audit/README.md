# `audit/` — Verification Output

Reserved for runtime audit artifacts written by the pipeline:
- `provenance.json`, `verification-report.json` will be emitted here on build.

Populated during build execution with verification logs and source provenance data.
