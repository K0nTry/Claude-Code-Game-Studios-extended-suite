# `simulation/` — Minimal State Simulation

Reserved for the Phase 16a whitebox / choice-tree → card-mode simulator output and run artifacts.
Populated at build time by the pipeline runner (seed=42 deterministic).
