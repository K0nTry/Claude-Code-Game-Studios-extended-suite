# `exports/` — Export Targets

Pipeline exports produced under feature-flag control:

- `yarn/`: Yarn dialogue format exports
- `nodes/`: Node-graph exports

These are generated only when the corresponding export flag is enabled (Phase 14b).
